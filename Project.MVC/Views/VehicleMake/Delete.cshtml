@model Project.MVC_.ViewModels.VehicleMakeViewModel

@{
    ViewBag.Title = "Delete Vehicle Make";
}

<h2>Delete Vehicle Make</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Vehicle Make</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.Name)</dt>
        <dd>@Html.DisplayFor(model => model.Name)</dd>

        <dt>@Html.DisplayNameFor(model => model.Abrv)</dt>
        <dd>@Html.DisplayFor(model => model.Abrv)</dd>
    </dl>

    @using (Html.BeginForm("Delete", "VehicleMake", new { id = Model.Id }, FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <input type="submit" value="Delete" class="btn btn-danger" />
        @Html.Raw(" | ")
        @Html.ActionLink("Back to List", "Index")
    }
</div>